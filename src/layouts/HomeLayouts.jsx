import React from 'react';
import Navbar from '../components/Navbar';

const HomeLayouts = () => {
    return (
        <div>
            <nav>
                <Navbar></Navbar>
            </nav>
            
        </div>
    );
};

export default HomeLayouts;